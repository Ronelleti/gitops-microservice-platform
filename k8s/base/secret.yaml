# Placeholder only — do NOT commit real secrets.
# In a real environment, use Sealed Secrets, SOPS, or AWS Secrets Manager
# with the External Secrets Operator instead of a plain Secret manifest.
apiVersion: v1
kind: Secret
metadata:
  name: microservice-secret
type: Opaque
stringData:
  EXAMPLE_API_KEY: "changeme"
