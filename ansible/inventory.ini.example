# Copy to inventory.ini and fill in the real values.
# Get the IP from `terraform output instance_public_ip` in terraform-free-tier/.
[webservers]
free-tier-host ansible_host=203.0.113.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/gitops-free-tier.pem
