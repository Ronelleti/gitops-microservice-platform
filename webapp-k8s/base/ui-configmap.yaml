apiVersion: v1
kind: ConfigMap
metadata:
  name: ui-config
data:
  # Overridden per environment in each overlay - must be a URL the
  # *browser* can reach (an Ingress hostname), never the internal
  # cluster-DNS service name.
  API_BASE_URL: "http://api.example.com"
