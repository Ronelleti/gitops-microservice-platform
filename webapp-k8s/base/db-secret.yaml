# Placeholder only — do NOT commit real credentials.
# Use Sealed Secrets, SOPS, or External Secrets Operator in a real setup.
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
type: Opaque
stringData:
  POSTGRES_DB: webapp
  POSTGRES_USER: webapp
  POSTGRES_PASSWORD: changeme
  DB_HOST: db
  DB_PORT: "5432"
  DB_NAME: webapp
  DB_USER: webapp
  DB_PASSWORD: changeme
