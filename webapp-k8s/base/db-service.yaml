apiVersion: v1
kind: Service
metadata:
  name: db
spec:
  clusterIP: None  # headless - StatefulSets address their pod directly
  selector:
    app: db
  ports:
    - port: 5432
      targetPort: 5432
